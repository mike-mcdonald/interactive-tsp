import { ActionTree } from 'vuex';
import axios from 'axios';
import { MapState } from './types';
import { RootState } from '../types';


export const actions: ActionTree<MapState, RootState> = {
    toggleLayerVisibility({ commit, state }, layerId) {

    }
};